name: sdkwork_rtc_flutter_mobile_core
description: SDKWork RTC Flutter Mobile Core - SDK factories, token manager, route registry
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
