name: sdkwork_rtc_flutter_mobile_rtc
description: SDKWork RTC Flutter Mobile RTC - rooms, media sessions, participants
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
