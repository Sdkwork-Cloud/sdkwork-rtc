# SDKWork RTC H5 Admin Core

H5移动端管理后台核心模块，提供Provider配置管理的Service、类型定义和组件。

## 功能

- Provider配置Schema加载
- Provider Account CRUD
- Provider Application CRUD
- Provider Credential CRUD
- Provider Profile CRUD
- Provider Route管理
- 移动端适配的Schema表单组件

## 依赖

- `@sdkwork/rtc-h5-core` - RTC核心SDK客户端
