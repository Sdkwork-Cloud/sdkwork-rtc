# Adaptive Web roots for sdkwork-rtc (copy into runtime config when in-process).
# Docker entrypoint generates /etc/sdkwork/webserver/module-app-roots/sdkwork-rtc.toml

[app_roots]
tablet_surface = "pc"

[app_roots.pc_static_by_environment]
development = "apps/sdkwork-rtc-pc/dist/dev"
test = "apps/sdkwork-rtc-pc/dist/test"
staging = "apps/sdkwork-rtc-pc/dist/staging"
production = "apps/sdkwork-rtc-pc/dist/prod"

[app_roots.h5_static_by_environment]
development = "apps/sdkwork-rtc-h5/dist/dev"
test = "apps/sdkwork-rtc-h5/dist/test"
staging = "apps/sdkwork-rtc-h5/dist/staging"
production = "apps/sdkwork-rtc-h5/dist/prod"

[app_roots.static_fallback_by_environment]
development = "deployments/webserver/static"
test = "deployments/webserver/static"
staging = "deployments/webserver/static"
production = "deployments/webserver/static"
