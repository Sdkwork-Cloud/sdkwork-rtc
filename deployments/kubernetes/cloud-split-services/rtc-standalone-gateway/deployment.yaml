apiVersion: apps/v1
kind: Deployment
metadata:
  name: rtc-standalone-gateway
  namespace: sdkwork-rtc
  labels:
    app.kubernetes.io/name: rtc-standalone-gateway
    app.kubernetes.io/component: standalone-gateway
    app.kubernetes.io/part-of: sdkwork-rtc
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: rtc-standalone-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rtc-standalone-gateway
        app.kubernetes.io/component: standalone-gateway
    spec:
      containers:
        - name: rtc-standalone-gateway
          image: ghcr.io/sdkwork/rtc-standalone-gateway:latest
          imagePullPolicy: IfNotPresent
          command:
            - /opt/sdkwork/rtc/bin/sdkwork-rtc-standalone-gateway
          ports:
            - name: http
              containerPort: 18088
          envFrom:
            - configMapRef:
                name: rtc-standalone-gateway-runtime
            - secretRef:
                name: rtc-standalone-gateway-secrets
                optional: true
          env:
            - name: SDKWORK_RTC_ENVIRONMENT
              value: production
            - name: SDKWORK_RTC_DEPLOYMENT_PROFILE
              value: cloud
            - name: SDKWORK_RTC_SERVICE_LAYOUT
              value: split-services
            - name: SDKWORK_RTC_APPLICATION_PUBLIC_INGRESS_BIND
              value: 0.0.0.0:18088
            - name: OTEL_SERVICE_NAME
              value: sdkwork-rtc-standalone-gateway
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
