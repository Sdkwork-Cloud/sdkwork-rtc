apiVersion: v1
kind: Service
metadata:
  name: rtc-api-server
  namespace: sdkwork-rtc
  labels:
    app.kubernetes.io/name: rtc-api-server
    app.kubernetes.io/component: api-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rtc-api-server
  ports:
    - name: http
      port: 18088
      targetPort: http
      protocol: TCP
